build/
compile_commands.json
logs/
build-*/
measure.txt

# приватний ключ вендора — НІКОЛИ в git
tools/vendor_ed25519_private.pem
tools/vendor_ed25519_public.pem

# провізійний SSH-ключ (приватний) — не комітити
tools/provision_key

# секретний ключ HMAC для підпису оновлень — тільки локально, НІКОЛИ в git
# (той самий, що на станції в /etc/vrx-update.key)
tools/update/vrx-update.key

# python bytecode
tools/__pycache__/
*.pyc

# альтернативні каталоги збірки
build-*/
# рантайм-стан пресетів екранів (код генерує дефолти)
screen*.json

# версія збірки: лічильник і згенерований рядок — локальний стан машини
# збірки (як build/). major.txt НАВПАКИ комітиться — він сталий і руками.
version/build_counter.txt
version/version.txt
