# Плеєр записів — узгоджені рішення

## Що робимо

Перегляд власних записів **у тій самій програмі**, що й приймання. Не
окремий додаток: станція далі приймає ефір і **пише його на носій**, а
змінюється лише те, що показує один із екранів.

Натиснув кнопку плеєра на екрані — цей екран перестає показувати ефір і
показує плеєр. Другий екран при цьому живе своїм життям: на ньому може
лишатись ефір, а може бути **свій, незалежний плеєр із іншим сеансом**.

## Узгоджені рішення

**Сеанс — одиниця перегляду.** Спершу список сеансів за часом вмикання
станції, тобто за фактом увімкнення. Обрав — унизу з'являється таймлайн.

**Вікна — ті самі, що в ефірі.** Три канали (основний, PiP, захват) у
вікнах, які рухаються й масштабуються мишею так само, як у головній
програмі. **Три власні пресети й три кнопки** — окремим набором від
ефірних: розкладка для перегляду й розкладка для польоту це різні речі.

**Телеметрія показується.** Поруч із кожним записом основного каналу вже
лежить `.ass` із телеметрією того ж моменту — плеєр малює її поверх
картинки. Вимикається **тією ж кнопкою OSD**, що й в основній програмі:
для того, хто дивиться, це та сама дія, і другої кнопки для неї бути не
має.

**Поточний сеанс відкривається теж.** Той, що зараз пишеться, — миттєвий
повтор: щойно щось сталося, відмотав і подивився, не перериваючи запису.

**Керування повне:** пуск/стоп, покадровий крок, швидкість **0.2–10×**,
стрибки **±0.5 / ±1 / ±5 / ±10 секунд**.

**Висока швидкість — пропуском кадрів.** Декодувати все на 10× ні до чого:
беруться лише опорні кадри. Для MJPEG-каналів опорний кожен, тож там це
безкоштовно.

## Що заміряно перед проєктуванням

Виміряно на станції, під час роботи й запису трьох каналів:

| що | значення |
|---|---|
| декод 1080p60 H265 | 1607 кадр/с = **26.8× реального часу** |
| демукс усього файлу (271 МБ) | 0.3 с |
| читання з носія повз кеш | 309 МБ/с |

Висновок: **вузького місця немає ні в декодері, ні в носії.** Десятикратна
швидкість повним декодом — це 600 кадр/с проти наявних 1607. Перемотка
назад, яка на H265 вимагає дочитати від опорного кадру, коштує частки
секунди. Кілька одночасних плеєрів у цей бюджет вкладаються.

## Перемотка: журнал замість таблиці переходів

Головна технічна опора. Файли пишуться з `streamable=true` — **таблиці
переходів у них немає взагалі**, навіть у штатно закритих (перевірено:
`Duration N/A`, `Cues` відсутні). Це свідома ціна за те, щоб файл пережив
висмикнуту флешку.

Але журнал сеансу кожні 5 секунд пише пару **`pts_s` + `bytes`**. Це рівно
та таблиця, якої бракує контейнеру: за часом одразу відомий байтовий
зсув. Перемотка стає стрибком у потрібне місце файлу плюс дочитуванням
уперед щонайбільше п'яти секунд — а декодер їх проковтує за 0.2 с.

Журнал писався заради вирівнювання каналів. Індексом перемотки він
виявився вже потім, і це найдешевша його властивість: рядок і так пишеться.

**Для поточного сеансу** остання мітка потрібна ще й як **межа читання**:
далі неї дані ще можуть не дійти до носія. Плеєр не заходить за неї — і
тоді миттєвий повтор не спотикається об недописаний хвіст.

## Проба перемотки — пройдена

Найризикованіше місце перевірено на справжньому записі до того, як писати
код. Питання стояло так: демультиплексор не побачить заголовка, якщо
почати читати з середини файлу, — чи можна згодувати йому початок файлу, а
далі підставити байти з потрібного зсуву.

Можна. Заміряно на файлі 271 МБ (163 с, 1080p60 H265):

- **заголовок займає 391 байт** — усе, що треба тримати в пам'яті, щоб
  почати з будь-якого місця;
- склейка «заголовок + кластер із середини» розбирається і ffmpeg, і
  GStreamer, причому **абсолютний час зберігається**: перший кадр
  склеєного віддав `PTS 82.966` — рівно середина запису;
- ланцюжок `matroskademux ! h265parse ! mppvideodec` проковтнув її **без
  жодної помилки чи попередження**: 4873 кадри за 3.1 с.

Тобто перемотка зводиться до: взяти байтовий зсув із журналу, від нього
знайти найближчий початок кластера вперед, і подати в `appsrc` 391 байт
заголовка плюс файл із того місця. Тимчасових файлів не потрібно.

**Одне уточнення для реалізації.** Початок кластера шукається за
чотирибайтовою міткою, а вона в принципі може випадково трапитись і
всередині даних. Кандидата треба перевіряти — розбирати його поле часу й
дивитись, чи воно осмислене; не зійшлося, шукаємо далі.

## Конструкція

**Роль екрана отримує режим:** ефір або плеєр. Малює той самий рендерер,
вікна ті самі — змінюється лише те, звідки беруться кадри: з UDP-джерела
чи з файлового.

**Запис це не чіпає.** Рекордери читають UDP і про показ не знають нічого;
перемикання екрана в плеєр для них не подія.

**Плеєр — накладний шар**, як OSD чи кнопки, і малюється лише на своїй
ролі: список сеансів, таймлайн, кнопки керування.

## Порядок робіт

1. Файлове джерело кадрів із перемоткою за журналом. Найризикованіше —
   робиться й міряється першим.
2. Режим ролі й перемикання екрана кнопкою.
3. Список сеансів і таймлайн.
4. Пресети плеєра, три кнопки.
5. Телеметрія з `.ass`.
6. Повне керування: швидкості, покадрово, стрибки.
